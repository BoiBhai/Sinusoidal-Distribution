pt_colors <- rgb(s_norm, k_norm, 0)
plot(sk_valid$skew, sk_valid$kurt,
ylim = c(-1, 4), xlim = c(-1, 2),
col = pt_colors, pch = 19, cex = 0.8,
xlab='Skewness', ylab='Kurtosis')
grid(lty = "dotted", col = "gray")
# 3. 2D Legend with Dense Ticks
par(mar = c(0.5, 0, 0, 4)) # Extra margin for the right-side labels
leg_res <- 100
leg_vals <- seq(0, 1, length.out = leg_res)
leg_grid <- expand.grid(s = leg_vals, k = leg_vals)
leg_colors <- rgb(leg_grid$s, leg_grid$k, 0)
# Generate the color gradient square
plot(leg_grid$s, leg_grid$k,
col = leg_colors, pch = 15, cex = 1.2,
xaxt = "n", yaxt = "n", xlab = "", ylab = "", bty = "n")
# --- TICK CONFIGURATION ---
# Define the density of ticks (e.g., every 0.1 units in parameter space)
tick_steps <- seq(0, 1, by = 0.1)
# Calculate the actual values for the labels based on your 'vals' range
s_range <- range(sk.grid$s)
k_range <- range(sk.grid$k)
s_labs <- round(seq(s_range[1], s_range[2], length.out = length(tick_steps)), 2)
k_labs <- round(seq(k_range[1], k_range[2], length.out = length(tick_steps)), 2)
# X-axis (s parameter)
axis(1, at = tick_steps, labels = s_labs, cex.axis = 0.7, las = 2)
mtext("s", side = 1, line = 3.5, cex = 0.9)
# Y-axis (k parameter - placed on the right)
axis(4, at = tick_steps, labels = k_labs, cex.axis = 0.7, las = 1)
mtext("k", side = 4, line = 2.5, cex = 0.9)
# Reset
layout(1)
# 1. Setup Layout
layout(matrix(1:2, ncol = 2), widths = c(4, 1.5))
par(mar = c(4, 4, 0, 0))
# 2. Main Plot (using your existing data)
s_norm <- (sk_valid$s - min(sk.grid$s)) / (max(sk.grid$s) - min(sk.grid$s))
k_norm <- (sk_valid$k - min(sk.grid$k)) / (max(sk.grid$k) - min(sk.grid$k))
pt_colors <- rgb(s_norm, k_norm, 0)
plot(sk_valid$skew, sk_valid$kurt,
ylim = c(-1, 4), xlim = c(-1, 2),
col = pt_colors, pch = 19, cex = 0.8,
xlab='Skewness', ylab='Kurtosis')
grid(lty = "dotted", col = "gray")
# 3. 2D Legend with Dense Ticks
par(mar = c(4, 0.5, 0, 4)) # Extra margin for the right-side labels
leg_res <- 100
leg_vals <- seq(0, 1, length.out = leg_res)
leg_grid <- expand.grid(s = leg_vals, k = leg_vals)
leg_colors <- rgb(leg_grid$s, leg_grid$k, 0)
# Generate the color gradient square
plot(leg_grid$s, leg_grid$k,
col = leg_colors, pch = 15, cex = 1.2,
xaxt = "n", yaxt = "n", xlab = "", ylab = "", bty = "n")
# --- TICK CONFIGURATION ---
# Define the density of ticks (e.g., every 0.1 units in parameter space)
tick_steps <- seq(0, 1, by = 0.1)
# Calculate the actual values for the labels based on your 'vals' range
s_range <- range(sk.grid$s)
k_range <- range(sk.grid$k)
s_labs <- round(seq(s_range[1], s_range[2], length.out = length(tick_steps)), 2)
k_labs <- round(seq(k_range[1], k_range[2], length.out = length(tick_steps)), 2)
# X-axis (s parameter)
axis(1, at = tick_steps, labels = s_labs, cex.axis = 0.7, las = 2)
mtext("s", side = 1, line = 3.5, cex = 0.9)
# Y-axis (k parameter - placed on the right)
axis(4, at = tick_steps, labels = k_labs, cex.axis = 0.7, las = 1)
mtext("k", side = 4, line = 2.5, cex = 0.9)
# Reset
layout(1)
# 1. Setup Layout
layout(matrix(1:2, ncol = 2), widths = c(4, 1.5))
par(mar = c(4, 4, 0, 0))
# 2. Main Plot (using your existing data)
s_norm <- (sk_valid$s - min(sk.grid$s)) / (max(sk.grid$s) - min(sk.grid$s))
k_norm <- (sk_valid$k - min(sk.grid$k)) / (max(sk.grid$k) - min(sk.grid$k))
pt_colors <- rgb(s_norm, k_norm, 0)
plot(sk_valid$skew, sk_valid$kurt,
ylim = c(-1, 4), xlim = c(-1, 2),
col = pt_colors, pch = 19, cex = 0.8,
xlab='Skewness', ylab='Kurtosis')
grid(lty = "dotted", col = "gray")
# 3. 2D Legend with Dense Ticks
par(mar = c(4, 0.5, 0, 4)) # Extra margin for the right-side labels
leg_res <- 100
leg_vals <- seq(0, 1, length.out = leg_res)
leg_grid <- expand.grid(s = leg_vals, k = leg_vals)
leg_colors <- rgb(leg_grid$s, leg_grid$k, 0)
# Generate the color gradient square
plot(leg_grid$s, leg_grid$k,
col = leg_colors, pch = 15, cex = 1.2,
xaxt = "n", yaxt = "n", xlab = "", ylab = "", bty = "n")
# --- TICK CONFIGURATION ---
# Define the density of ticks (e.g., every 0.1 units in parameter space)
tick_steps <- seq(0, 1, by = 0.1)
# Calculate the actual values for the labels based on your 'vals' range
s_range <- range(sk.grid$s)
k_range <- range(sk.grid$k)
s_labs <- round(seq(s_range[1], s_range[2], length.out = length(tick_steps)), 2)
k_labs <- round(seq(k_range[1], k_range[2], length.out = length(tick_steps)), 2)
# X-axis (s parameter)
axis(1, at = tick_steps, labels = s_labs, cex.axis = 0.7, las = 2)
mtext("s", side = 1, line = 2.5, cex = 0.9)
# Y-axis (k parameter - placed on the right)
axis(4, at = tick_steps, labels = k_labs, cex.axis = 0.7, las = 1)
mtext("k", side = 4, line = 2.5, cex = 0.9)
# Reset
layout(1)
# 1. Setup Layout
layout(matrix(1:2, ncol = 2), widths = c(4, 1.5))
par(mar = c(4, 4, 0, 0))
# 2. Main Plot (using your existing data)
s_norm <- (sk_valid$s - min(sk.grid$s)) / (max(sk.grid$s) - min(sk.grid$s))
k_norm <- (sk_valid$k - min(sk.grid$k)) / (max(sk.grid$k) - min(sk.grid$k))
pt_colors <- rgb(s_norm, k_norm, 0)
plot(sk_valid$skew, sk_valid$kurt,
ylim = c(-1, 4), xlim = c(-1, 2),
col = pt_colors, pch = 19, cex = 0.8,
xlab='Skewness', ylab='Kurtosis')
grid(lty = "dotted", col = "gray")
# 3. 2D Legend with Dense Ticks
par(mar = c(4, 0.5, 0, 4)) # Extra margin for the right-side labels
leg_res <- 100
leg_vals <- seq(0, 1, length.out = leg_res)
leg_grid <- expand.grid(s = leg_vals, k = leg_vals)
leg_colors <- rgb(leg_grid$s, leg_grid$k, 0)
# Generate the color gradient square
plot(leg_grid$s, leg_grid$k,
col = leg_colors, pch = 15, cex = 1.2,
xaxt = "n", yaxt = "n", xlab = "", ylab = "", bty = "n")
# --- TICK CONFIGURATION ---
# Define the density of ticks (e.g., every 0.1 units in parameter space)
tick_steps <- seq(0, 1, by = 0.1)
# Calculate the actual values for the labels based on your 'vals' range
s_range <- range(sk.grid$s)
k_range <- range(sk.grid$k)
s_labs <- round(seq(s_range[1], s_range[2], length.out = length(tick_steps)), 2)
k_labs <- round(seq(k_range[1], k_range[2], length.out = length(tick_steps)), 2)
# X-axis (s parameter)
axis(1, at = tick_steps, labels = s_labs, cex.axis = 0.7, las = 2)
mtext("s", side = 1, line = 2, cex = 0.9)
# Y-axis (k parameter - placed on the right)
axis(4, at = tick_steps, labels = k_labs, cex.axis = 0.7, las = 1)
mtext("k", side = 4, line = 2, cex = 0.9)
# Reset
layout(1)
# 1. Setup Layout
layout(matrix(1:2, ncol = 2), widths = c(4, 1.5))
par(mar = c(4, 4, 0, 0))
# 2. Main Plot (using your existing data)
s_norm <- (sk_valid$s - min(sk.grid$s)) / (max(sk.grid$s) - min(sk.grid$s))
k_norm <- (sk_valid$k - min(sk.grid$k)) / (max(sk.grid$k) - min(sk.grid$k))
pt_colors <- rgb(s_norm, k_norm, 0)
plot(sk_valid$skew, sk_valid$kurt,
ylim = c(-1, 4), xlim = c(-1, 2),
col = pt_colors, pch = 19, cex = 0.8,
xlab='Skewness', ylab='Kurtosis')
grid(lty = "dotted", col = "gray")
# 3. 2D Legend with Dense Ticks
par(mar = c(3.5, 0.5, 0, 3)) # Extra margin for the right-side labels
leg_res <- 100
leg_vals <- seq(0, 1, length.out = leg_res)
leg_grid <- expand.grid(s = leg_vals, k = leg_vals)
leg_colors <- rgb(leg_grid$s, leg_grid$k, 0)
# Generate the color gradient square
plot(leg_grid$s, leg_grid$k,
col = leg_colors, pch = 15, cex = 1.2,
xaxt = "n", yaxt = "n", xlab = "", ylab = "", bty = "n")
# --- TICK CONFIGURATION ---
# Define the density of ticks (e.g., every 0.1 units in parameter space)
tick_steps <- seq(0, 1, by = 0.1)
# Calculate the actual values for the labels based on your 'vals' range
s_range <- range(sk.grid$s)
k_range <- range(sk.grid$k)
s_labs <- round(seq(s_range[1], s_range[2], length.out = length(tick_steps)), 2)
k_labs <- round(seq(k_range[1], k_range[2], length.out = length(tick_steps)), 2)
# X-axis (s parameter)
axis(1, at = tick_steps, labels = s_labs, cex.axis = 0.7, las = 2)
mtext("s", side = 1, line = 2, cex = 0.9)
# Y-axis (k parameter - placed on the right)
axis(4, at = tick_steps, labels = k_labs, cex.axis = 0.7, las = 1)
mtext("k", side = 4, line = 2, cex = 0.9)
# Reset
layout(1)
# 1. Setup Layout
layout(matrix(1:2, ncol = 2), widths = c(4, 1.5))
par(mar = c(4, 4, 0, 0))
# 2. Main Plot (using your existing data)
s_norm <- (sk_valid$s - min(sk.grid$s)) / (max(sk.grid$s) - min(sk.grid$s))
k_norm <- (sk_valid$k - min(sk.grid$k)) / (max(sk.grid$k) - min(sk.grid$k))
pt_colors <- rgb(s_norm, k_norm, 0)
plot(sk_valid$skew, sk_valid$kurt,
ylim = c(-1, 4), xlim = c(-1, 2),
col = pt_colors, pch = 19, cex = 0.8,
xlab='Skewness', ylab='Kurtosis')
grid(lty = "dotted", col = "gray")
# 3. 2D Legend with Dense Ticks
par(mar = c(3.5, 0.5, -0.2, 2.5)) # Extra margin for the right-side labels
# 1. Setup Layout
layout(matrix(1:2, ncol = 2), widths = c(4, 1.5))
par(mar = c(4, 4, 0, 0))
# 2. Main Plot (using your existing data)
s_norm <- (sk_valid$s - min(sk.grid$s)) / (max(sk.grid$s) - min(sk.grid$s))
k_norm <- (sk_valid$k - min(sk.grid$k)) / (max(sk.grid$k) - min(sk.grid$k))
pt_colors <- rgb(s_norm, k_norm, 0)
plot(sk_valid$skew, sk_valid$kurt,
ylim = c(-1, 4), xlim = c(-1, 2),
col = pt_colors, pch = 19, cex = 0.8,
xlab='Skewness', ylab='Kurtosis')
grid(lty = "dotted", col = "gray")
# 3. 2D Legend with Dense Ticks
par(mar = c(3.5, 0.5, -0.2, 3.5)) # Extra margin for the right-side labels
# 1. Setup Layout
layout(matrix(1:2, ncol = 2), widths = c(4, 1.5))
par(mar = c(4, 4, 0, 0))
# 2. Main Plot (using your existing data)
s_norm <- (sk_valid$s - min(sk.grid$s)) / (max(sk.grid$s) - min(sk.grid$s))
k_norm <- (sk_valid$k - min(sk.grid$k)) / (max(sk.grid$k) - min(sk.grid$k))
pt_colors <- rgb(s_norm, k_norm, 0)
plot(sk_valid$skew, sk_valid$kurt,
ylim = c(-1, 4), xlim = c(-1, 2),
col = pt_colors, pch = 19, cex = 0.8,
xlab='Skewness', ylab='Kurtosis')
grid(lty = "dotted", col = "gray")
# 3. 2D Legend with Dense Ticks
par(mar = c(3.5, 0.5, 0, 3.5)) # Extra margin for the right-side labels
leg_res <- 100
leg_vals <- seq(0, 1, length.out = leg_res)
leg_grid <- expand.grid(s = leg_vals, k = leg_vals)
leg_colors <- rgb(leg_grid$s, leg_grid$k, 0)
# Generate the color gradient square
plot(leg_grid$s, leg_grid$k,
col = leg_colors, pch = 15, cex = 1.2,
xaxt = "n", yaxt = "n", xlab = "", ylab = "", bty = "n")
# --- TICK CONFIGURATION ---
# Define the density of ticks (e.g., every 0.1 units in parameter space)
tick_steps <- seq(0, 1, by = 0.1)
# Calculate the actual values for the labels based on your 'vals' range
s_range <- range(sk.grid$s)
k_range <- range(sk.grid$k)
s_labs <- round(seq(s_range[1], s_range[2], length.out = length(tick_steps)), 2)
k_labs <- round(seq(k_range[1], k_range[2], length.out = length(tick_steps)), 2)
# X-axis (s parameter)
axis(1, at = tick_steps, labels = s_labs, cex.axis = 0.7, las = 2)
mtext("s", side = 1, line = 2, cex = 0.9)
# Y-axis (k parameter - placed on the right)
axis(4, at = tick_steps, labels = k_labs, cex.axis = 0.7, las = 1)
mtext("k", side = 4, line = 2, cex = 0.9)
# Reset
layout(1)
# 1. Setup Layout
layout(matrix(1:2, ncol = 2), widths = c(4, 1.5))
par(mar = c(4, 4, 0, 0))
# 2. Main Plot (using your existing data)
s_norm <- (sk_valid$s - min(sk.grid$s)) / (max(sk.grid$s) - min(sk.grid$s))
k_norm <- (sk_valid$k - min(sk.grid$k)) / (max(sk.grid$k) - min(sk.grid$k))
pt_colors <- rgb(s_norm, k_norm, 0)
plot(sk_valid$skew, sk_valid$kurt,
ylim = c(-1, 4), xlim = c(-1, 2),
col = pt_colors, pch = 19, cex = 0.8,
xlab='Skewness', ylab='Kurtosis')
grid(lty = "dotted", col = "gray")
# 3. 2D Legend with Dense Ticks
par(mar = c(3.5, 0.5, 0, 3)) # Extra margin for the right-side labels
leg_res <- 100
leg_vals <- seq(0, 1, length.out = leg_res)
leg_grid <- expand.grid(s = leg_vals, k = leg_vals)
leg_colors <- rgb(leg_grid$s, leg_grid$k, 0)
# Generate the color gradient square
plot(leg_grid$s, leg_grid$k,
col = leg_colors, pch = 15, cex = 1.2,
xaxt = "n", yaxt = "n", xlab = "", ylab = "", bty = "n")
# --- TICK CONFIGURATION ---
# Define the density of ticks (e.g., every 0.1 units in parameter space)
tick_steps <- seq(0, 1, by = 0.1)
# Calculate the actual values for the labels based on your 'vals' range
s_range <- range(sk.grid$s)
k_range <- range(sk.grid$k)
s_labs <- round(seq(s_range[1], s_range[2], length.out = length(tick_steps)), 2)
k_labs <- round(seq(k_range[1], k_range[2], length.out = length(tick_steps)), 2)
# X-axis (s parameter)
axis(1, at = tick_steps, labels = s_labs, cex.axis = 0.7, las = 2)
mtext("s", side = 1, line = 2, cex = 0.9)
# Y-axis (k parameter - placed on the right)
axis(4, at = tick_steps, labels = k_labs, cex.axis = 0.7, las = 1)
mtext("k", side = 4, line = 2, cex = 0.9)
# Reset
layout(1)
# 1. Setup Layout
layout(matrix(1:2, ncol = 2), widths = c(4, 1.5))
par(mar = c(4, 4, 0, 0))
# 2. Main Plot (using your existing data)
s_norm <- (sk_valid$s - min(sk.grid$s)) / (max(sk.grid$s) - min(sk.grid$s))
k_norm <- (sk_valid$k - min(sk.grid$k)) / (max(sk.grid$k) - min(sk.grid$k))
pt_colors <- rgb(s_norm, k_norm, 0)
plot(sk_valid$skew, sk_valid$kurt,
ylim = c(-1, 4), xlim = c(-1, 2),
col = pt_colors, pch = 19, cex = 0.8,
xlab='Skewness', ylab='Kurtosis')
grid(lty = "dotted", col = "gray")
# 3. 2D Legend with Dense Ticks
par(mar = c(3, 0.5, 0, 3)) # Extra margin for the right-side labels
leg_res <- 100
leg_vals <- seq(0, 1, length.out = leg_res)
leg_grid <- expand.grid(s = leg_vals, k = leg_vals)
leg_colors <- rgb(leg_grid$s, leg_grid$k, 0)
# Generate the color gradient square
plot(leg_grid$s, leg_grid$k,
col = leg_colors, pch = 15, cex = 1.2,
xaxt = "n", yaxt = "n", xlab = "", ylab = "", bty = "n")
# --- TICK CONFIGURATION ---
# Define the density of ticks (e.g., every 0.1 units in parameter space)
tick_steps <- seq(0, 1, by = 0.1)
# Calculate the actual values for the labels based on your 'vals' range
s_range <- range(sk.grid$s)
k_range <- range(sk.grid$k)
s_labs <- round(seq(s_range[1], s_range[2], length.out = length(tick_steps)), 2)
k_labs <- round(seq(k_range[1], k_range[2], length.out = length(tick_steps)), 2)
# X-axis (s parameter)
axis(1, at = tick_steps, labels = s_labs, cex.axis = 0.7, las = 2)
mtext("s", side = 1, line = 2, cex = 0.9)
# Y-axis (k parameter - placed on the right)
axis(4, at = tick_steps, labels = k_labs, cex.axis = 0.7, las = 1)
mtext("k", side = 4, line = 2, cex = 0.9)
# Reset
layout(1)
dev.new(width = 8, height = 6, noRStudioGD = TRUE)
# 1. Setup Layout
layout(matrix(1:2, ncol = 2), widths = c(4, 1.5))
par(mar = c(4, 4, 0, 0))
# 2. Main Plot (using your existing data)
s_norm <- (sk_valid$s - min(sk.grid$s)) / (max(sk.grid$s) - min(sk.grid$s))
k_norm <- (sk_valid$k - min(sk.grid$k)) / (max(sk.grid$k) - min(sk.grid$k))
pt_colors <- rgb(s_norm, k_norm, 0)
plot(sk_valid$skew, sk_valid$kurt,
ylim = c(-1, 4), xlim = c(-1, 2),
col = pt_colors, pch = 19, cex = 0.8,
xlab='Skewness', ylab='Kurtosis')
grid(lty = "dotted", col = "gray")
# 3. 2D Legend with Dense Ticks
par(mar = c(3, 0.5, 0, 3)) # Extra margin for the right-side labels
leg_res <- 100
leg_vals <- seq(0, 1, length.out = leg_res)
leg_grid <- expand.grid(s = leg_vals, k = leg_vals)
leg_colors <- rgb(leg_grid$s, leg_grid$k, 0)
# Generate the color gradient square
plot(leg_grid$s, leg_grid$k,
col = leg_colors, pch = 15, cex = 1.2,
xaxt = "n", yaxt = "n", xlab = "", ylab = "", bty = "n")
# --- TICK CONFIGURATION ---
# Define the density of ticks (e.g., every 0.1 units in parameter space)
tick_steps <- seq(0, 1, by = 0.1)
# Calculate the actual values for the labels based on your 'vals' range
s_range <- range(sk.grid$s)
k_range <- range(sk.grid$k)
s_labs <- round(seq(s_range[1], s_range[2], length.out = length(tick_steps)), 2)
k_labs <- round(seq(k_range[1], k_range[2], length.out = length(tick_steps)), 2)
# X-axis (s parameter)
axis(1, at = tick_steps, labels = s_labs, cex.axis = 0.7, las = 2)
mtext("s", side = 1, line = 2, cex = 0.9)
# Y-axis (k parameter - placed on the right)
axis(4, at = tick_steps, labels = k_labs, cex.axis = 0.7, las = 1)
mtext("k", side = 4, line = 2, cex = 0.9)
# Reset
layout(1)
#dev.copy2pdf(file='2-dqf.pdf')
#dev.off()
dev.new(width = 8, height = 6, noRStudioGD = TRUE)
# 1. Setup Layout
layout(matrix(1:2, ncol = 2), widths = c(4, 1.5))
par(mar = c(4, 4, 0, 0))
# 2. Main Plot (using your existing data)
s_norm <- (sk_valid$s - min(sk.grid$s)) / (max(sk.grid$s) - min(sk.grid$s))
k_norm <- (sk_valid$k - min(sk.grid$k)) / (max(sk.grid$k) - min(sk.grid$k))
pt_colors <- rgb(s_norm, k_norm, 0)
plot(sk_valid$skew, sk_valid$kurt,
ylim = c(-1, 4), xlim = c(-1, 2),
col = pt_colors, pch = 19, cex = 0.8,
xlab='Skewness', ylab='Kurtosis')
grid(lty = "dotted", col = "gray")
# 3. 2D Legend with Dense Ticks
par(mar = c(3, 0.5, 0, 3)) # Extra margin for the right-side labels
leg_res <- 100
leg_vals <- seq(0, 1, length.out = leg_res)
leg_grid <- expand.grid(s = leg_vals, k = leg_vals)
leg_colors <- rgb(leg_grid$s, leg_grid$k, 0)
# Generate the color gradient square
plot(leg_grid$s, leg_grid$k,
col = leg_colors, pch = 15, cex = 1.2,
xaxt = "n", yaxt = "n", xlab = "", ylab = "", bty = "n")
# --- TICK CONFIGURATION ---
# Define the density of ticks (e.g., every 0.1 units in parameter space)
tick_steps <- seq(0, 1, by = 0.1)
# Calculate the actual values for the labels based on your 'vals' range
s_range <- range(sk.grid$s)
k_range <- range(sk.grid$k)
s_labs <- round(seq(s_range[1], s_range[2], length.out = length(tick_steps)), 2)
k_labs <- round(seq(k_range[1], k_range[2], length.out = length(tick_steps)), 2)
# X-axis (s parameter)
axis(1, at = tick_steps, labels = s_labs, cex.axis = 0.7, las = 2)
mtext("s", side = 1, line = 2, cex = 0.9)
# Y-axis (k parameter - placed on the right)
axis(4, at = tick_steps, labels = k_labs, cex.axis = 0.7, las = 1)
mtext("k", side = 4, line = 2, cex = 0.9)
# Reset
layout(1)
dev.copy2pdf(file='2-skewkurtcover.pdf')
dev.off()
par(mfrow=c(5,1))
for (k in c(0.5, 1, 2, 3, 4)){
curve((sin(pi*x))^k)
}
par(mfrow=c(5,1), mar=c(2,2,0,0))
for (k in c(0.5, 1, 2, 3, 4)){
curve((sin(pi*x))^k)
}
par(mfrow=c(5,1), mar=c(2,2,0,0))
for (k in c(4,3,2,1,0.5)){
curve((sin(pi*x))^k)
}
par(mfcol=c(5,1), mar=c(2,2,0,0))
for (k in c(4,3,2,1,0.5)){
curve((sin(pi*x))^k)
}
par(mfcol=c(5,1), mar=c(2,2,0,0))
for (k in c(4,3,2,1,0.5)){
curve((sin(pi*x))^k)
}
par(mfrow=c(1,5), mar=c(2,2,0,0))
for (k in c(4,3,2,1,0.5)){
curve((sin(pi*x))^k)
}
dev.new(width=5, height=1)
par(mfrow=c(1,5), mar=c(2,2,0,0))
for (k in c(4,3,2,1,0.5)){
curve((sin(pi*x))^k)
}
dev.new(width=5, height=1, noRStudioGD = TRUE)
par(mfrow=c(1,5), mar=c(2,2,0,0))
for (k in c(4,3,2,1,0.5)){
curve((sin(pi*x))^k)
}
dev.new(width=25, height=5, noRStudioGD = TRUE)
par(mfrow=c(1,5), mar=c(2,2,0,0))
for (k in c(4,3,2,1,0.5)){
curve((sin(pi*x))^k)
}
dev.new(width=25, height=5, noRStudioGD = TRUE)
par(mfrow=c(1,5), mar=c(2,2,0,0))
for (k in c(4,3,2,1,0.5)){
curve((sin(pi*x))^k, main=paste0('sin^', k, '(pi*x)'))
}
dev.new(width=25, height=5, noRStudioGD = TRUE)
par(mfrow=c(1,5), mar=c(2,4,0,0))
for (k in c(4,3,2,1,0.5)){
curve((sin(pi*x))^k, main=paste0('sin^', k, '(pi*x)'))
}
dev.new(width=25, height=5, noRStudioGD = TRUE)
par(mfrow=c(1,5), mar=c(4,2,0,0))
for (k in c(4,3,2,1,0.5)){
curve((sin(pi*x))^k, main=paste0('sin^', k, '(pi*x)'))
}
dev.new(width=25, height=5, noRStudioGD = TRUE)
par(mfrow=c(1,5), mar=c(2,2,0,0))
for (k in c(4,3,2,1,0.5)){
curve((sin(pi*x))^k, main=paste0('sin^', k, '(pi*x)'))
}
dev.new(width=25, height=5, noRStudioGD = TRUE)
par(mfrow=c(1,5), mar=c(2,2,0,0))
for (k in c(0.5,1,2,3,4)){
curve((sin(pi*x))^k, main=paste0('sin^', k, '(pi*x)'))
}
dev.new(width=25, height=5, noRStudioGD = TRUE)
par(mfrow=c(1,5), mar=c(2,2,0,0))
for (k in c(0.5,1,2,3,4)){
curve((sin(pi*x))^k))
dev.new(width=25, height=5, noRStudioGD = TRUE)
par(mfrow=c(1,5), mar=c(2,2,0,0))
for (k in c(0.5,1,2,3,4)){
curve((sin(pi*x))^k)
}
dev.new(width=25, height=5, noRStudioGD = TRUE)
par(mfrow=c(1,5), mar=c(2,2,0,0))
for (k in c(0.5,1,2,3,4)){
curve((sin(pi*x))^k)
}
source("D:/GitHub/Sinusoidal-Distribution/r/presp1-sineshapes.R")
dev.new(width=25, height=5, noRStudioGD = TRUE)
par(mfrow=c(1,5), mar=c(2,2,0,0))
for (k in c(0.5,1,2,3,4)){
curve((sin(pi*x))^k)
}
dev.copy2pdf(file='p1-sinek.pdf')
par(mfrow=c(1,5), mar=c(2,2,0,0))
for (s in c(0.1,0.5,1,2,3)){
curve((sin(pi*x))^k)
}
dev.copy2pdf(file='p1-sines.pdf')
dev.off()
dev.new(width=25, height=5, noRStudioGD = TRUE)
par(mfrow=c(1,5), mar=c(2,2,0,0))
for (k in c(0.5,1,2,3,4)){
curve((sin(pi*x))^k)
}
dev.copy2pdf(file='p1-sinek.pdf')
par(mfrow=c(1,5), mar=c(2,2,0,0))
for (s in c(0.1,0.5,1,2,3)){
curve((sin(pi*x^s)))
}
dev.copy2pdf(file='p1-sines.pdf')
dev.off()
source("D:/GitHub/Sinusoidal-Distribution/r/presp1-sineshapes.R")
